## Getting Started

create .env from .env.example and add require googel and github credential for auth before running the mini-project

```bash
pnpm install 
```
use this script to install dependencies

To run the development server:

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.


